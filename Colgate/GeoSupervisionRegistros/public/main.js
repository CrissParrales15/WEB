import { obtenerSupervisor } from "./js/select-funciones.js";

window.addEventListener("DOMContentLoaded", () => {
  // Cargamos los Supervisores obtenidos de la Base de Datos
  obtenerSupervisor();
});
